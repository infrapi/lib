# Invalid configuration for error handling demonstration
# This file contains invalid values that will trigger validation errors

# Missing required field: INFRAPI_APP_NAME

# Invalid log level (valid: trace, debug, info, error)
INFRAPI_APP_LOG_LEVEL="invalid"

# Invalid platform (valid: sandbox, preprod, prod)
INFRAPI_APP_PLATFORM="invalid"

# Invalid region (valid: eu-west-1, eu-west-2, au-southeast-1)
INFRAPI_APP_REGION="invalid"
